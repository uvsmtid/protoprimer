
TODO: TODO_73_71_31_84.sub_command_check_or_info.md

Add `SubCommand.command_info` or `SubCommand.command_check`?

Effectively, it could be the "dry run" mode.
We can specify which `StateStride` or which `EnvState` to check things until,
we can make it non-failing for multiple use cases to check if things are correct.

Examples for `info`:
*   Print script path and version.
*   Print python path and version.
*   Maybe something from `log_python_context`?
*   Also, print `venv` info.
*   Also, print `EnvVar.*`.
*   Make it possible to work at any `StateStride`.
