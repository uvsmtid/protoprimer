
TODO: TODO_31_76_38_60.sub_command_for_shell.md

Add new `SubCommand.run_mode`:

*   Do we want to replace `--command` switch by `SubCommand.run_mode` or `SubCommand.shell_mode` which executes whatever is passed on CLI or from `stdin`?

    Or we still use `-c`/`--command` just like any shell does?

*   The new `SubCommand.run_mode` or `SubCommand.shell_mode` will be used for all executions in `shell`.

*   Do we even need `venv_shell` after that?
