
TODO: TODO_31_76_38_60.exec_operation_for_shell.md

Add new `ExecOperation.run_mode`:

*   Do we want to replace `--command` switch by `ExecOperation.run_mode` or `ExecOperation.shell_mode` which executes whatever is passed on CLI or from `stdin`?

    Or we still use `-c`/`--command` just like any shell does?

*   The new `ExecOperation.run_mode` or `ExecOperation.shell_mode` will be used for all executions in `shell`.

*   Do we even need `venv_shell` after that?
