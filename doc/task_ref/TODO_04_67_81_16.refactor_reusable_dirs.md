
TODO: TODO_04_67_81_16.refactor_reusable_dirs.md

Rename `TopDir` enum to `ReusableDir`
and align it with the spec [reusable_dir][FT_20_13_95_11.reusable_dir.md]:

*   Rename `cache` to `gen`.

*   Reconfigure all of them to stay under `./var/` dir (except `./venv/` dir).

*   Reconfigure all of them to stay under `./protoprimer/` sub-dir.

*   Start using `./run/` for pid files.

[FT_20_13_95_11.reusable_dir.md]: ../feature_topic/FT_20_13_95_11.reusable_dir.md
