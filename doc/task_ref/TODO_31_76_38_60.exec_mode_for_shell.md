
TODO: TODO_31_76_38_60.exec_mode_for_shell.md

Maybe we need to refactor CLI?
*   DONE: Rename `prime` mode to `boot` mode (still default).
*   DONE: Rename `upgrade` to `reset` mode (relates to `boot`).
*   DONE: Rename `config` to `eval`.

Add new `ExecMode.run_mode`:
*   Replace `--command` switch by `ExecMode.run_mode` which executes whatever is passed on CLI or from `stdin`.
*   The new `ExecMode.run_mode` will be used for all executions in `shell`.
*   Change `venv_shell` to use `start_app`.
