
TODO: TODO_31_76_38_60.exec_mode_for_shell.md

Add new `ExecMode.run_mode`:

*   Do we want to replace `--command` switch by `ExecMode.run_mode` or `ExecMode.shell_mode` which executes whatever is passed on CLI or from `stdin`?

    Or we still use `-c`/`--command` just like any shell does?

*   The new `ExecMode.run_mode` or `ExecMode.shell_mode` will be used for all executions in `shell`.

*   Do we even need `venv_shell` after that?
