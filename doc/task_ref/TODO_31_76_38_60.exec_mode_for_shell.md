
TODO: TODO_31_76_38_60.exec_mode_for_shell.md

Maybe we need to refactor CLI?
*   Rename `prime` mode to `boot` mode (still default).
*   Rename `upgrade` to `reset` mode (relates to `boot`) or to `nuke` (relates to `prime`).
*   Rename `config` to `eval` or `resolve` (avoid `re-` clash in `reset` and `resolve` - one of them should be different).
*   Replace `--command` switch by `ExecMode.run_mode` which executes whatever is passed on CLI or from `stdin`.
*   The new `exec_mode` will be used for all executions in `shell`.
*   Change `venv_shell` to use `start_app`.
*   Add `info`? Prints script path & version, python path & version, something else?
