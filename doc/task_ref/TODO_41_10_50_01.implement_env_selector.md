
TODO: TODO_41_10_50_01.implement_env_selector.md:

There should be a way to delegate selection of the env to target repo.

TODO: Create FT (feature_topic) and document in pseudocode how env selection happens with rules of precedence:
*   When env selector says one thing, but existing symlink points to another - who wins?
*   When default env is configured, but existing symlink points to another - who wins?

TODO: The bottom line, in order to be able to "push" changes (and affect existing deployments),
      local symlink should not matter (overwrite by decision),
      unless `--env` arg is used (which is technically, the highest priority).
      Does it mean that local symlink will be fine only when all
      return `None` (`--env` arg, selector script, default config)?
      But...
TODO: In case of non-bootstrapping sub-commands like `eval`,
      the value must be read from symlink.
      If `eval` allows `--env`, then even `eval` has to adjust the symlink (which is not read-only).

Similar to `python` selector - see:

*   `SelectorFunc.select_python_file_abs_path`
*   `select_python_file_abs_path`
*   `ConfField.field_python_selector_file_rel_path`
*   `EnvState.state_python_selector_file_abs_path_inited`

TODO: Also, document `python` selector - create an FT (feature_topic).
