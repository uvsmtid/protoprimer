---
use_case: UC_81_50_97_17
case_title: reuse logger
case_status: TEST
---

A started app (see [FT_05_08_64_67.start_app.md][FT_05_08_64_67.start_app.md]) should be able
to reuse the existing logger set up by the primer (possibly re-configuring it).

TODO: Shell we disable configuring loggers for `EntryFunc.func_start_app`?
It cannot reuse loggers because the main function is executed
immediately after the process switches (via `os.execv`) (without bootstrap).
And it cannot configure it identically because the env vars are removed
before passing control to the main function.

[FT_05_08_64_67.start_app.md]: ../feature_topic/FT_05_08_64_67.start_app.md
