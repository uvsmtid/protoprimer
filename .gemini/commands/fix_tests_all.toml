
description = "Runs all tests (see FT_83_60_72_19.test_perimeter.md / pytest_all.run.xml)"

prompt = """
Run `pytest` (as is, you are in `venv`) with `CI` env var set to `true` and adjust failing tests.

Output like:

```
TEST_EXIT[1]
```

does not indicate a failure (with non-zero exit code 1) - it just logs test exit with that code.
Use real `pytest` exit code to decide whether tests pass or not.
"""
